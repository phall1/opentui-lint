/** @jsxImportSource @opentui/solid */
// The React spelling of a compound element: solid's catalogue only knows
// ascii_font, so this is the "wrong binding" branch, not a plain unknown tag.
const App = () => <ascii-font text="HI" />;
export default App;
