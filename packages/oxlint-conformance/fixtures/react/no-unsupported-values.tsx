/** @jsxImportSource @opentui/react */
// A negative dimension is the one case whose message names the reconciler's
// ErrorBoundary explicitly, which makes it the clearest per-binding check.
const App = () => <box width={-1} />;
export default App;
