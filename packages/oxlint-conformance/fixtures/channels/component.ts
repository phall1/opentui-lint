/** @jsxImportSource @opentui/react */
// Byte-for-byte the same violation as cli-entry.ts, at a filename that does
// NOT match the allowInFiles pattern — the pair proves context.filename
// reaches the rule and is not a placeholder.
process.stdout.write("boot\n");
