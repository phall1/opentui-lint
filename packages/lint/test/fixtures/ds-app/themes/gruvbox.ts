import type { ThemeDefinition } from "../components/ui/theme"

/** A preset. Forty lines of raw hex, all of it correct. */
export const gruvbox: ThemeDefinition = {
  tokens: {
    colors: { background: "#282828", surface: "#3C3836", foreground: "#EBDBB2", border: "#665C54", primary: "#83A598" },
  },
}
