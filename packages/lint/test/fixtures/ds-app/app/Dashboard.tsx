/** Consumer code. This is the file the design-system rules exist to check. */
import { Button } from "@/components/ui/button";

export const Dashboard = () => (
  <box paddingX={1}>
    <Button label="Deploy" />
  </box>
);
