// Placeholder. RuleTester supplies real source text per case via `code`,
// overriding this file's content the same way an editor's unsaved buffer
// overrides disk — this just needs to exist so the tsconfig's `include` glob
// picks it up and @typescript-eslint/parser builds a program around it.
export const App = () => <box />
